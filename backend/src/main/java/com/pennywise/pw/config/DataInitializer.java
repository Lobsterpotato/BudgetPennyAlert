package com.pennywise.pw.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataInitializer {
    // Removed mock data initialization to ensure users are created through signup
    // and to avoid conflicts with the frontend authentication flow
}
